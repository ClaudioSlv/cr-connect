import { AppShell } from "@/components/app-shell";import { SubscriptionManager } from "@/components/subscription-manager";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";
export default async function SubscriptionPage(){const db=await createClient();const{data:{user}}=await db.auth.getUser();if(!user)redirect("/login");const{data}=await db.from("workshop_users").select("workshop_id,role,workshops(name)").eq("user_id",user.id).limit(1).maybeSingle();if(!data)redirect("/app");const workshop=data.workshops as unknown as{name:string};return <AppShell workshop={workshop.name}><p className="text-xs font-bold tracking-[.2em] text-[#FFC107]">CR CONNECT</p><h1 className="mt-2 text-3xl font-bold">Assinatura</h1><p className="mt-2 text-zinc-400">Escolha o plano da sua oficina e acompanhe sua assinatura.</p><div className="mt-7"><SubscriptionManager workshopId={data.workshop_id} isAdmin={data.role==="admin"}/></div></AppShell>}
