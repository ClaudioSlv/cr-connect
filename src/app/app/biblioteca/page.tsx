import{AppShell}from"@/components/app-shell";import{DiagnosticLibrary}from"@/components/diagnostic-library";import{createClient}from"@/lib/supabase/server";import{redirect}from"next/navigation";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data}=await s.from("workshop_users").select("workshop_id,workshops(name)").eq("user_id",user!.id).limit(1).maybeSingle();if(!data)redirect("/app");const w=data.workshops as unknown as{name:string};return <AppShell workshop={w.name}><p className="text-xs font-bold tracking-[.2em] text-[#FFC107]">CONHECIMENTO TÉCNICO</p><h1 className="mt-2 text-3xl font-bold">Biblioteca Técnica</h1><p className="mt-2 text-zinc-400">Procedimentos, referências e informações da sua oficina.</p><div className="mt-7"><DiagnosticLibrary workshopId={data.workshop_id} mode="tech"/></div></AppShell>}
