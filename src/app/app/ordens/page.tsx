import { AppShell } from "@/components/app-shell"; import { ServiceOrdersManager } from "@/components/service-orders-manager"; import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation";
export default async function OrdersPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data}=await s.from("workshop_users").select("workshop_id,workshops(name)").eq("user_id",user!.id).limit(1).maybeSingle();if(!data)redirect("/app");const w=data.workshops as unknown as {name:string};return <AppShell workshop={w.name}><p className="text-xs font-bold tracking-[.2em] text-[#FFC107]">ATENDIMENTO</p><h1 className="mt-2 text-3xl font-bold">Ordens de Serviço</h1><p className="mt-2 text-zinc-400">Registre o atendimento e acompanhe cada reparo.</p><div className="mt-7"><ServiceOrdersManager workshopId={data.workshop_id}/></div></AppShell>}
