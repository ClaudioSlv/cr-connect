"use client";
import Image from "next/image";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function LoginPage(){const[email,setEmail]=useState("");const[notice,setNotice]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const{error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setNotice(error?error.message:"Verifique seu e-mail para continuar.");setBusy(false);}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-[#5a461c] bg-[#121212] p-8 shadow-2xl"><Image src="/brand/cr-reparador.jpg" width={160} height={160} alt="CR Reparador Automotivo" className="mx-auto rounded-2xl"/><p className="mt-5 text-center text-sm font-bold tracking-[.18em] text-[#FFC107]">CR CONNECT</p><h1 className="mt-3 text-center text-3xl font-bold">Bem-vindo de volta</h1><p className="mt-2 text-center text-sm text-zinc-400">Use seu e-mail para receber um link seguro de acesso.</p><label className="mt-7 block text-sm">E-mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="field mt-2" placeholder="E-mail ou telefone"/></label><button disabled={busy} className="mt-5 w-full rounded-lg bg-[#FFC107] px-4 py-3 font-bold text-black disabled:opacity-60">{busy?"Enviando...":"Entrar"}</button>{notice&&<p className="mt-4 text-sm text-zinc-300">{notice}</p>}</form></main>}
