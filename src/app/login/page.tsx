"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function LoginPage(){const[email,setEmail]=useState("");const[notice,setNotice]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const{error}=await createClient().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setNotice(error?error.message:"Verifique seu e-mail para continuar.");setBusy(false);}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md rounded-xl bg-[#1A1A1A] p-8 shadow-2xl"><p className="text-sm font-bold tracking-[.18em] text-[#FFC107]">CR CONNECT</p><h1 className="mt-3 text-3xl font-bold">Acessar oficina</h1><p className="mt-2 text-sm text-zinc-400">Use seu e-mail para receber um link seguro de acesso.</p><label className="mt-7 block text-sm">E-mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-950 px-3 py-3 outline-none focus:border-[#FFC107]"/></label><button disabled={busy} className="mt-5 w-full rounded-md bg-[#FFC107] px-4 py-3 font-semibold text-black disabled:opacity-60">{busy?"Enviando...":"Enviar link de acesso"}</button>{notice&&<p className="mt-4 text-sm text-zinc-300">{notice}</p>}</form></main>}
