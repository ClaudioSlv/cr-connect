"use client";
import { FormEvent, useState } from "react";
export function DiagnosticAssistant(){const[vehicle,setVehicle]=useState(""),[symptom,setSymptom]=useState(""),[answer,setAnswer]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function send(e:FormEvent){e.preventDefault();setLoading(true);setError("");setAnswer("");const r=await fetch("/api/diagnostico-ia",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({vehicle,symptom})});const data=await r.json();if(!r.ok)setError(data.error||"Falha ao consultar o assistente.");else setAnswer(data.answer);setLoading(false)}return <div className="grid gap-7 xl:grid-cols-[420px_1fr]"><form onSubmit={send} className="rounded-xl border border-zinc-800 bg-[#1A1A1A] p-5"><h2 className="font-semibold">Descreva o caso</h2><input className="field mt-4" placeholder="Veículo: marca, modelo, ano e motor" value={vehicle} onChange={e=>setVehicle(e.target.value)}/><textarea required className="field mt-3 min-h-44" placeholder="Ex.: carro falha ao acelerar, luz da injeção acesa, começou após abastecer..." value={symptom} onChange={e=>setSymptom(e.target.value)}/><button disabled={loading} className="mt-4 rounded-lg bg-[#FFC107] px-4 py-2 font-bold text-black disabled:opacity-60">{loading?"Analisando...":"Analisar sintoma"}</button></form><section className="rounded-xl border border-[#4a3818] bg-[#171717] p-5"><p className="text-xs font-bold tracking-[.16em] text-[#FFC107]">ANÁLISE ASSISTIDA</p>{!answer&&!error&&<p className="mt-5 text-zinc-400">Informe os sintomas para receber hipóteses e verificações iniciais.</p>}{error&&<p className="mt-5 text-red-300">{error}</p>}{answer&&<p className="mt-5 whitespace-pre-wrap leading-7 text-zinc-200">{answer}</p>}<p className="mt-6 border-t border-zinc-800 pt-4 text-xs text-zinc-500">A IA auxilia a triagem, mas não substitui o diagnóstico presencial de um profissional.</p></section></div>}
