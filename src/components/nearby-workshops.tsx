"use client";
import { useState } from "react";import { createClient } from "@/lib/supabase/client";
type Workshop={id:string;name:string;latitude:number;longitude:number;emergency_services:string[];emergency_radius_km:number;distance?:number};
export function NearbyWorkshops(){const db=createClient();const[items,setItems]=useState<Workshop[]>([]),[message,setMessage]=useState("Use sua localização para encontrar oficinas ativas.");const[loading,setLoading]=useState(false);function distance(a:number,b:number,c:number,d:number){const r=6371,x=(c-a)*Math.PI/180,y=(d-b)*Math.PI/180;const q=Math.sin(x/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(y/2)**2;return 2*r*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}function locate(){if(!navigator.geolocation){setMessage("Seu navegador não oferece GPS.");return}setLoading(true);navigator.geolocation.getCurrentPosition(async p=>{const{data,error}=await db.rpc("get_active_sos_workshops");if(error){setMessage("Não foi possível buscar oficinas agora.");setLoading(false);return}const list=((data||[])as Workshop[]).map(x=>({...x,distance:distance(p.coords.latitude,p.coords.longitude,Number(x.latitude),Number(x.longitude))})).filter(x=>(x.distance||0)<=x.emergency_radius_km).sort((a,b)=>(a.distance||0)-(b.distance||0));setItems(list);setMessage(list.length?"Oficinas encontradas perto de você.":"Nenhuma oficina CR SOS ativa no seu raio agora.");setLoading(false)},()=>{setMessage("Permita a localização para usar o CR SOS.");setLoading(false)},{enableHighAccuracy:true,timeout:10000})}return <div><button onClick={locate} disabled={loading} className="rounded-lg bg-[#FFC107] px-5 py-3 font-bold text-black disabled:opacity-60">{loading?"Buscando...":"Usar minha localização"}</button><p className="mt-4 text-zinc-400">{message}</p><div className="mt-6 space-y-3">{items.map(item=><article key={item.id} className="rounded-xl border border-zinc-800 bg-[#171717] p-5"><div className="flex flex-wrap justify-between gap-4"><div><h2 className="text-lg font-bold">{item.name}</h2><p className="mt-1 text-sm text-zinc-400">{item.emergency_services.join(" · ")}</p><b className="mt-3 block text-[#FFC107]">{item.distance?.toFixed(1)} km de você</b></div><a className="h-fit rounded-lg border border-[#FFC107] px-4 py-2 font-bold text-[#FFC107]" href={`https://www.google.com/maps/dir/?api=1&destination=${item.latitude},${item.longitude}`} target="_blank" rel="noreferrer">Abrir rota</a></div></article>)}</div></div>}
