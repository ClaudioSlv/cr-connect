"use client";
import { FormEvent,useState } from "react";import { createClient } from "@/lib/supabase/client";
type Workshop={id:string;name:string;latitude:number;longitude:number;emergency_services:string[];emergency_radius_km:number;distance?:number};
export function NearbyWorkshops(){const db=createClient();const[items,setItems]=useState<Workshop[]>([]),[message,setMessage]=useState("Use sua localização para encontrar oficinas ativas."),[loading,setLoading]=useState(false),[position,setPosition]=useState<{latitude:number;longitude:number}|null>(null),[selected,setSelected]=useState<Workshop|null>(null),[name,setName]=useState(""),[phone,setPhone]=useState(""),[service,setService]=useState(""),[description,setDescription]=useState("");function distance(a:number,b:number,c:number,d:number){const r=6371,x=(c-a)*Math.PI/180,y=(d-b)*Math.PI/180,q=Math.sin(x/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(y/2)**2;return 2*r*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}function locate(){if(!navigator.geolocation){setMessage("Seu navegador não oferece GPS.");return}setLoading(true);navigator.geolocation.getCurrentPosition(async p=>{const current={latitude:p.coords.latitude,longitude:p.coords.longitude};const{data,error}=await db.rpc("get_active_sos_workshops");if(error){setMessage("Não foi possível buscar oficinas agora.");setLoading(false);return}const list=((data||[])as Workshop[]).map(item=>({...item,distance:distance(current.latitude,current.longitude,Number(item.latitude),Number(item.longitude))})).filter(item=>(item.distance||0)<=item.emergency_radius_km).sort((a,b)=>(a.distance||0)-(b.distance||0));setPosition(current);setItems(list);setMessage(list.length?"Oficinas encontradas perto de você.":"Nenhuma oficina CR SOS ativa no seu raio agora.");setLoading(false)},()=>{setMessage("Permita a localização para usar o CR SOS.");setLoading(false)},{enableHighAccuracy:true,timeout:10000})}async function requestHelp(event:FormEvent){event.preventDefault();if(!selected||!position)return;setLoading(true);const{error}=await db.rpc("create_sos_request",{p_workshop_id:selected.id,p_requester_name:name,p_requester_phone:phone,p_service_type:service,p_description:description,p_latitude:position.latitude,p_longitude:position.longitude});setLoading(false);if(error){setMessage("Não foi possível enviar seu chamado. Tente novamente.");return}setMessage(`Chamado enviado para ${selected.name}. A oficina foi avisada.`);setSelected(null);setName("");setPhone("");setService("");setDescription("")}return <div><button onClick={locate} disabled={loading} className="rounded-lg bg-[#FFC107] px-5 py-3 font-bold text-black disabled:opacity-60">{loading?"Buscando...":"Usar minha localização"}</button><p className="mt-4 text-zinc-400">{message}</p><div className="mt-6 space-y-3">{items.map(item=><article key={item.id} className="rounded-xl border border-zinc-800 bg-[#171717] p-5"><div className="flex flex-wrap justify-between gap-4"><div><h2 className="text-lg font-bold">{item.name}</h2><p className="mt-1 text-sm text-zinc-400">{item.emergency_services.join(" · ")}</p><b className="mt-3 block text-[#FFC107]">{item.distance?.toFixed(1)} km de você</b></div><div className="flex h-fit gap-2"><button onClick={()=>{setSelected(item);setService(item.emergency_services[0]||"")}} className="rounded-lg bg-[#FFC107] px-4 py-2 font-bold text-black">Pedir ajuda</button><a className="rounded-lg border border-[#FFC107] px-4 py-2 font-bold text-[#FFC107]" href={`https://www.google.com/maps/dir/?api=1&destination=${item.latitude},${item.longitude}`} target="_blank" rel="noreferrer">Abrir rota</a></div></div></article>)}</div>{selected&&<form onSubmit={requestHelp} className="mt-6 rounded-xl border border-[#FFC107] bg-[#171717] p-5"><h2 className="text-xl font-bold">Pedir ajuda para {selected.name}</h2><p className="mt-1 text-sm text-zinc-400">A oficina receberá seu telefone, localização e descrição.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><input required className="field" placeholder="Seu nome" value={name} onChange={e=>setName(e.target.value)}/><input required className="field" type="tel" placeholder="Seu telefone" value={phone} onChange={e=>setPhone(e.target.value)}/><select required className="field" value={service} onChange={e=>setService(e.target.value)}>{selected.emergency_services.map(option=><option key={option} value={option}>{option}</option>)}</select><textarea className="field min-h-24" placeholder="Descreva o problema (opcional)" value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="mt-4 flex gap-3"><button disabled={loading} className="rounded-lg bg-[#FFC107] px-4 py-2 font-bold text-black">{loading?"Enviando...":"Enviar chamado"}</button><button type="button" onClick={()=>setSelected(null)} className="rounded-lg border border-zinc-600 px-4 py-2 font-bold">Cancelar</button></div></form>}</div>}
