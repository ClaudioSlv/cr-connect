"use client";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";
type Notification = { id: string; title: string; body: string | null; read_at: string | null; created_at: string };
export function NotificationsManager({ workshopId, userId }: { workshopId: string; userId: string }) { const db = createClient(); const [items, setItems] = useState<Notification[]>([]); async function load() { const { data } = await db.from("notifications").select("id,title,body,read_at,created_at").eq("workshop_id", workshopId).eq("user_id", userId).order("created_at", { ascending: false }); setItems((data || []) as Notification[]); } useEffect(() => { load(); }, []); async function markRead(id: string) { await db.from("notifications").update({ read_at: new Date().toISOString() }).eq("id", id); load(); } async function markAllRead() { await db.from("notifications").update({ read_at: new Date().toISOString() }).eq("workshop_id", workshopId).eq("user_id", userId).is("read_at", null); load(); } const unread = items.filter(item => !item.read_at).length; return <section><div className="flex flex-wrap items-center justify-between gap-3"><p className="text-zinc-400">{unread ? `${unread} aviso${unread > 1 ? "s" : ""} não lido${unread > 1 ? "s" : ""}` : "Tudo em dia."}</p>{unread > 0 && <button onClick={markAllRead} className="rounded-lg border border-[#FFC107] px-4 py-2 text-sm font-bold text-[#FFC107]">Marcar tudo como lido</button>}</div><div className="mt-5 space-y-3">{items.length === 0 && <div className="rounded-xl border border-dashed border-zinc-700 p-6 text-zinc-400">Nenhuma notificação ainda. Novos avisos aparecerão aqui.</div>}{items.map(item => <article key={item.id} className={`rounded-xl border p-5 ${item.read_at ? "border-zinc-800 bg-[#171717]" : "border-[#6b510d] bg-[#211805]"}`}><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-semibold">{item.title}</h2>{item.body && <p className="mt-2 text-sm text-zinc-400">{item.body}</p>}<time className="mt-3 block text-xs text-zinc-500">{new Date(item.created_at).toLocaleString("pt-BR")}</time></div>{!item.read_at && <button onClick={() => markRead(item.id)} className="h-fit rounded-lg bg-[#FFC107] px-3 py-2 text-sm font-bold text-black">Lida</button>}</div></article>)}</div></section>; }
